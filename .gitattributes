# Normalize line endings so CRLF/LF drift can never perturb the byte-identical
# fixtures and snapshot tests that back PatchDrill's determinism guarantee.
* text=auto eol=lf
*.png binary
*.jpg binary
*.gif binary
*.ico binary
